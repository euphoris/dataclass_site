# 1일차 종합 실습

- 엔진에 들어가는 고강도 커넥터 'TX-100'을 신규 생산 라인(라인 3)에서 양산
- TX-100 커넥터 두께의 설계 규격은 5.00mm ± 0.05mm
- 라인 3 가동 초기 일주일간 생산된 TX-100 커넥터의 두께 데이터를 수집
- 최근 조립 라인에서 일부 TX-100 커넥터가 규격 미달 또는 초과로 인해 조립 불량을 유발한다는 보고가 접수
- 라인 3의 공정이 안정적으로 운영되고 있는지, 그리고 조립 불량을 유발할 만한 이상 데이터(Outlier)가 존재하는지 통계적으로 검토하고자 함

[📁 실습파일 다운로드](https://github.com/euphoris/datasets/raw/refs/heads/master/TX100.xlsx)

| 컬럼 | 설명 |
| --- | --- |
| day | 측정일(1~7) |
| measurement_id | 해당 일 내의 측정 순번(1~50) |
| thickness | 측정된 커넥터 두께(mm) |


import { QuizComponent } from "@/components/QuizComponent";

<QuizComponent quizId="day1-exercise" quizItems={[
    {
        "item_type": "number",
        "question": "라인 3에서 생산된 TX-100 커넥터 두께의 평균 (소숫점 이하 3자리까지)",
        "options": [],
        "tolerance": 0.001,
        "hint": "```\nimport pandas as pd\ndf = pd.read_excel(\'TX100.xlsx\')\ndf.head()\ndf.thickness.mean()\n```",
        "solution": 5.003608571428571
    },
    {
        "item_type": "number",
        "question": "중간값 (소숫점 이하 3자리까지)",
        "tolerance": 0.001,
        "hint": "```\ndf.thickness.median()\n```",
        "solution": 5.004
    },
    {
        "item_type": "number",
        "question": "범위 (소숫점 이하 3자리까지)",
        "tolerance": 0.001,
        "hint": "```\ndf.thickness.max() - df.thickness.min()\n```",
        "solution": 0.1899999999999995
    },
    {
        "item_type": "number",
        "question": "IQR (소숫점 이하 3자리까지)",
        "tolerance": 0.001,
        "hint": "```\nQ1 = df.thickness.quantile(0.25)\nQ3 = df.thickness.quantile(0.75)\nIQR = Q3 - Q1\n```",
        "solution": 0.019000000000000128
    },
    {
        "item_type": "number",
        "question": "표준편차 (소숫점 이하 3자리까지)",
        "tolerance": 0.001,
        "hint": "```\ndf.thickness.std()\n```",
        "solution": 0.01749362342162191
    },
    {
        "item_type": "radio",
        "question": "bins=10으로 히스토그램을 그렸을 때 가장 빈도가 높은 구간",
        "options": [
            "(4.904, 4.924]",
            "(4.924, 4.943]",
            "(4.943, 4.962]",
            "(4.962, 4.981]",
            "(4.981, 5.0]",
            "(5.0, 5.019]",
            "(5.019, 5.038]",
            "(5.038, 5.057]",
            "(5.057, 5.076]",
            "(5.076, 5.095]"
        ],
        "hint": "```\nimport seaborn as sns\nsns.histplot(df.thickness, bins=10)\n```",
        "solution": "(5.0, 5.019]"
    },
    {
        "item_type": "number",
        "question": "Z-score를 이용해 탐지한 이상치의 개수",
        "options": [],
        "hint": "```\nm = df.thickness.mean()\ns = df.thickness.std()\ndf[\'Z_thickness\'] = (df.thickness - m) / s\ndf.query(\'Z_thickness.abs() > 3\').shape[0]\n```",
        "solution": 6
    },
    {
        "item_type": "number",
        "question": "IQR을 이용해 탐지한 이상치의 개수",
        "options": [],
        "hint": "```\nupper_bound = Q3 + 1.5 * IQR\nlower_bound = Q1 - 1.5 * IQR\ndf.query(\'thickness < @lower_bound or thickness > @upper_bound\').shape[0]\n```",
        "solution": 9
    },
]} />


## Q&A
<iframe src="https://tally.so/embed/wbOOKg?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=0" loading="lazy" width="100%" height="274" frameborder="0" marginheight="0" marginwidth="0" title="Q&A"></iframe>
