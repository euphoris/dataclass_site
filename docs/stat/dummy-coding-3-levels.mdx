# 범주가 3개인 경우

## 범주가 3개인 경우

*   ABC 순 `A` 기준 설정됨.
*   `B`, `C` 대한 더미 변수 2개(`TRT[T.B]`, `TRT[T.C]`) 생성.

| TRT | TRT[T.B] | TRT[T.C] |
| :-- | :------- | :------- |
| A   | 0        | 0        |
| B   | 1        | 0        |
| C   | 0        | 1        |

*   **Python 결과 (`ols('y ~ TRT', dep).fit().summary()`):**
    *   `Intercept`: 기준(`A`)의 평균 `y` (예: 62.3333).
    *   `TRT[T.B]`: `B` 평균 `y` - `A` 평균 `y` 차이 (예: -10.4167).
    *   `TRT[T.C]`: `C` 평균 `y` - `A` 평균 `y` 차이 (예: -11.0833).
    *   **B의 평균 y:** Intercept + TRT[T.B] 계수 = 62.3333 - 10.4167 = 51.9166.
    *   **C의 평균 y:** Intercept + TRT[T.C] 계수 = 62.3333 - 11.0833 = 51.2500.

## Python에서 기준 범주 바꾸기

*   `C()` 함수와 `Treatment()` 사용.
    ```python
    # model 변수의 기준 범주를 'K3'로 지정
    ols('price ~ C(model, Treatment("K3"))', df).fit().summary()
    ```


## 퀴즈

import { QuizComponent } from "@/components/QuizComponent";

<QuizComponent quizId="dummy-coding-3-levels" quizItems={[
    {
        "item_type": "radio",
        "question": "safety.xlsx에서 `risk ~ wheels`로 회귀분석을 해보세요. 이때 wheels의 기준 범주는 무엇입니까?",
        "options": [
            "4wd",
            "fwd",
            "rwd",
        ],
        "hint": "기준 범주는 알파벳 순서로 가장 앞에 오는 범주입니다",
        "solution": "4wd",
    },
    {
        "item_type": "radio",
        "question": "wheels[T.fwd]의 계수는 무엇을 의미합니까?",
        "options": [
            "4wd의 평균 risk",
            "fwd의 평균 risk",
            "rwd의 평균 risk",
            "fwd와 rwd의 평균 risk 차이",
            "fwd와 4wd의 평균 risk 차이",
            "4wd와 rwd의 평균 risk 차이",
        ],
        "hint": "더미 변수의 계수는 해당 범주와 기준 범주의 평균 차이를 의미합니다",
        "solution": "fwd와 4wd의 평균 risk 차이",
    },
    {
        "item_type": "radio",
        "question": "wheels[T.rwd]의 계수는 무엇을 의미합니까?",
        "options": [
            "4wd의 평균 risk",
            "fwd의 평균 risk",
            "rwd의 평균 risk",
            "fwd와 rwd의 평균 risk 차이",
            "fwd와 4wd의 평균 risk 차이",
            "4wd와 rwd의 평균 risk 차이",
        ],
        "hint": "더미 변수의 계수는 해당 범주와 기준 범주의 평균 차이를 의미합니다",
        "solution": "rwd와 4wd의 평균 risk 차이",
    },
    {
        "item_type": "radio",
        "question": "절편은 무엇을 의미합니까?",
        "options": [
            "4wd의 평균 risk",
            "fwd의 평균 risk",
            "rwd의 평균 risk",
            "fwd와 rwd의 평균 risk 차이",
            "fwd와 4wd의 평균 risk 차이",
            "4wd와 rwd의 평균 risk 차이",
        ],
        "hint": "더미 변수가 있는 회귀분석에서 절편은 기준 범주의 평균을 나타냅니다.",
        "solution": "4wd의 평균 risk",
    },
    {
        "item_type": "radio",
        "question": "위 회귀분석에서 통계적으로 유의한 차이가 **없는** 범주 쌍을 **모두** 고르세요. (유의수준 5%)",
        "options": [
            "fwd와 rwd",
            "fwd와 4wd",
            "4wd와 rwd"
        ],
        "hint": "더미코딩에서 더미 변수에 사용된 범주끼리는 차이가 있는지 없는지 알 수 없습니다",
        "solution": [
            "fwd와 4wd",
            "4wd와 rwd"
        ],
    },
]} />

## Q&A
<iframe src="https://tally.so/embed/wbOOKg?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=0" loading="lazy" width="100%" height="274" frameborder="0" marginheight="0" marginwidth="0" title="Q&A"></iframe>
