# 모듈과 파일

## 모듈 임포트 (Module Import)

*   **모듈(Module):** 관련된 함수들을 모아놓은 것 (일종의 코드 묶음).
*   모듈 함수 사용하려면 먼저 모듈 **임포트(import)** 필요.
    ```python
    import pandas as pd
    ```
*   `pandas`: 표 형태 데이터 다루는 대표적 모듈.
*   `as pd`: `pandas` 모듈을 앞으로 `pd`라는 **약칭(별명)**으로 부르겠다는 의미 (생략 가능하나 관례적으로 사용).

## 파일 열기 (Pandas 활용)

```python
df = pd.read_excel('car.xlsx')
```

*   `pd.` : `pd`(pandas 모듈)에 **소속된** 기능을 사용하겠다는 의미 (Python에서 `.`은 소속 나타냄).
*   `read_excel()` : 엑셀 파일 읽는 함수.
*   `'car.xlsx'` : 읽어올 파일 이름 (문자열이므로 따옴표 사용).
*   `df = ...` : 파일 읽은 내용(데이터프레임)을 `df`라는 변수에 할당.

## 데이터 보기

```python
df.head()
```

*   **객체 소속 함수(메서드):** Python에서는 특정 변수(객체)에 소속된 함수(메서드) 존재.
*   `df.head()`: `df` 변수(데이터프레임)에 소속된 `head` 메서드 호출. `df`의 **첫 5행** 보여주는 역할.
*   **괄호 `()`:** 함수(메서드) 호출 시에는 인자가 없어도 반드시 괄호 붙임.
*   **행 개수 지정:** `df.head(10)` 과 같이 괄호 안에 숫자를 넣어 보여줄 행 개수 지정 가능.

## 패키지 설치

*   **패키지(Package):** 모듈보다 더 큰 단위. 추가적인 함수/기능 묶음. (모듈과 혼용되기도 함)
*   **예시:** 통계 분석 위한 `pingouin` 패키지 설치.
*   **`pip` 사용:** Python 패키지 관리자 `pip` 프로그램 이용 설치.
```bash
pip install pingouin
```
*   위 명령어는 Jupyter Notebook 셀에서 실행하거나, Anaconda Prompt(터미널)에서 실행.

## 퀴즈


import { QuizComponent } from "@/components/QuizComponent";

<QuizComponent quizId="module" quizItems={[
    {
        item_type: 'checkbox',
        question: 'import pandas as pd에 대한 설명으로 올바른 것을 모두 고르세요',
        options: [
            'pandas라는 모듈을 불러들인다',
            'pd라는 약칭을 붙인다',
            'as는 항상 붙여야 한다',
            '함수 이름 앞에 pd.를 붙여야 한다',
            '한 노트북에서 한 번만 실행하면 된다',
        ],
        hint: '강의자료에서 모듈 임포트에 대한 설명을 확인하세요.',
        solution: [
            'pandas라는 모듈을 불러들인다',
            'pd라는 약칭을 붙인다',
            '함수 이름 앞에 pd.를 붙여야 한다',
            '한 노트북에서 한 번만 실행하면 된다',
        ],
    },
    {
        item_type: 'checkbox',
        question: "df = pd.read_excel('car.xlsx')에 대한 설명으로 올바른 것을 모두 고르세요",
        options: [
            'read_excel은 pandas 모듈에 소속된 함수이다',
            'car.xlsx 파일을 읽어온다',
            '파일 이름은 반드시 따옴표로 묶어야 한다',
            '작은 따옴표(\')는 큰 따옴표(\")로 바꿀 수 없다',
            '파일 내용은 df 변수에 저장된다',
        ],
        hint: 'Python에서 작은 따옴표 또는 큰 따옴표는 문자열(텍스트)를 나타냅니다.',
        solution: [
            'read_excel은 pandas 모듈에 소속된 함수이다',
            'car.xlsx 파일을 읽어온다',
            '파일 이름은 반드시 따옴표로 묶어야 한다',
            '파일 내용은 df 변수에 저장된다',
        ],
    },
    {
        item_type: 'short',
        question: "TX100.xlsx 파일을 열어보세요. 첫 열의 이름은 무엇인가요?",
        hint: '```\ntx = pd.read_excel("TX100.xlsx")\ntx.head()\n```\n를 사용해보세요.',
        solution: [
            'read_excel은 pandas 모듈에 소속된 함수이다',
            'car.xlsx 파일을 읽어온다',
            '파일 이름은 반드시 따옴표로 묶어야 한다',
            '파일 내용은 df 변수에 저장된다',
        ],
    },
]} />



## Q&A
<iframe src="https://tally.so/embed/wbOOKg?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=0" loading="lazy" width="100%" height="274" frameborder="0" marginheight="0" marginwidth="0" title="Q&A"></iframe>
