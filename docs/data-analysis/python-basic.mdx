
# Python 기본 문법

## Python을 계산기처럼 사용하기
* 덧셈 (+): `5 + 3` -> 8
* 뺄셈 (-): `10 - 4` -> 6
* 곱셈 (*): `7 * 6` -> 42
* 나눗셈 (/): `10 / 3` -> 3.333...
* 정수 나눗셈 (몫) (//): `10 // 3` -> 3
* 나머지 (%): `10 % 3` -> 1
* 거듭제곱 (**): `2 ** 4` -> 16 (2의 4제곱)

## 연산자 우선순위
* 연산자 우선순위: 일반적인 수학의 연산 순서와 유사
1. `**` (거듭제곱)
2. `-` (음수 부호)
3. `*` , `/`, `//`, `%`
4. `+` , `-`
* 예시: `2 + 3 * 4` -> `2 + 12` -> 14 (곱셈이 먼저)
* 괄호 안의 표현식이 가장 먼저 계산
* 예시 1: `(2 + 3) * 4` -> `5 * 4` -> 20 (괄호 안의 덧셈이 먼저)

## 변수
* 변수: 데이터를 저장하는 공간
* 데이터(값)에 이름을 붙이는 것 `x = 3`
* 변수는 값과 동일하게 사용할 수 있다 `x + 1`
* 한 번 정의한 변수는 다시 정의할 수 있다 `x = 4`
* =의 오른쪽을 계산 후 왼쪽에 대입 `x = x + 1`
* 변수 생성 규칙:
    * 한글, 영문자, 숫자, 밑줄(_) 사용 가능
    * 숫자로 시작할 수 없음(x1은 가능, 1x는 불가능)
    * 대소문자 구분
    * 예약어 사용 불가 (e.g., `if`, `for`, `while`)

## Variable Inspector
* Jupyter에 Variable Inspector 확장을 설치하면 변수를 쉽게 볼 수 있음
* Extension Manager 창
* Warning에는 Yes 클릭
* variable로 검색
* variable-inspector (파란색 아이콘)를 install
* 설치가 끝나면 새로 고침
* 왼쪽 (x) 아이콘을 클릭하면 변수 목록

## 주석
* 한 줄의 코드에서 `#` 를 표시 이후의 내용은 주석 처리
* 코드에 설명이나 메모 등을 남기기 위한 방법
```python
점심값 = 8000
인원수 = 5
점심값 * 인원수 # 부서 점심 식사
```
* 더 이상 사용하지 않지만 삭제하기는 곤란한 코드의 경우, 주석 처리하면 편리
```python
업무추진비 = 100000
# 업무추진비 - 점심값 * 인원수 # 업무추진비에서 차감
```
    * 위 코드는 업무추진비에서 점심 식사비를 차감하는 부분을 주석 처리
    * 나중에 차감하게 될 경우 #을 지워서 코드를 살릴 수 있음

## 함수
* 함수이름(인자) 형태로 사용.
```python
max(1, 2, 3) # 1, 2, 3 중에서 최대값
```
* Python에서는 다양한 함수들을 모듈(module)이라는 단위로 모아서 관리
    * 예: 수학 관련 모듈은 `math`
* 모듈을 사용하려면 먼저 `import` 문을 통해 모듈을 불러와야 함
```python
import math
```
* 모듈에 포함된 함수는 모듈.함수 형태로 사용
```python
math.sqrt(4) # √2
```
* 한 번 임포트한 모듈은 같은 노트북 안에서는 계속 사용할 수 있음(새 노트북에서는 별도로 임포트)
```python
math.sqrt(9) # √9
```

## from … import … 문법
* 자주 사용하는 함수의 경우, 매번 모듈.함수 형태로 사용하면 번거로움
* 모듈 안의 특정 함수만 from 모듈 import 함수 형태로 임포트할 수 있음
```python
from math import sin # math 모듈에서 sin 함수만 임포트
sin(0) # 사인 0도
```
* 여러 함수를 임포트할 수도 있음
```python
from math import sin, cos, tan # math 모듈에서 sin, cos, tan 함수만 임포트
cos(0) # 코사인 0도
```

## 퀴즈

import { QuizComponent } from "@/components/QuizComponent";

<QuizComponent quizId="python-basic" quizItems={

[
    {
        "item_type": "radio",
        "question": "Python에서 나눗셈의 몫을 구하는 정수 나눗셈 연산자는 무엇입니까?",
        "options": [
            "/",
            "%",
            "//",
            "**"
        ],
        "hint": "나눗셈 기호(/)를 두 번 연속으로 사용합니다.",
        "solution": "//"
    },
    {
        item_type: 'number',
        question: 'Python으로 7 ** 2 // 5 를 계산해보세요. 결과는 얼마입니까?',
        hint: '주피터 노트북에 코드를 입력하고 실행해보세요.',
        solution: 9
    },
    {
        item_type: 'radio',
        question: 'Python에서 a = 5의 의미는 무엇입니까?',
        options: [
            'a와 5가 같다',
            '변수 a에 5를 대입하라'
        ],
        hint: '`a = 5`는 변수 a에 값 5를 할당하는 것을 의미합니다.',
        solution: '변수 a에 5를 대입하라'
    },
    {
        item_type: 'checkbox',
        question: '변수에 대한 설명으로 옳은 것을 모두 고르세요',
        options: [
            '값에 이름을 붙인 것이다',
            '값과 동일하게 사용할 수 있다',
            '한 번 정의한 변수는 재정의할 수 없다',
        ],
        hint: '변수는 값에 이름을 붙여서, 값과 동일하게 사용할 수 있습니다.',
        solution: [
            '값에 이름을 붙인 것이다',
            '값과 동일하게 사용할 수 있다',
        ],
    },
    {
        "item_type": "checkbox",
        "question": "다음 중 Python의 변수 생성 규칙으로 올바른 것을 모두 고르세요.",
        "options": [
            "변수 이름은 숫자로 시작할 수 있다.",
            "변수 이름은 대소문자를 구분한다.",
            "예약어(if, for 등)는 변수 이름으로 사용할 수 있다.",
            "변수 이름에 한글을 사용할 수 있다."
        ],
        "hint": "교안에 따르면 변수 이름은 한글, 영문자, 숫자, 밑줄(_)로 구성될 수 있지만, 숫자로 시작할 수는 없습니다. 또한 대소문자를 구별합니다.",
        "solution": [
            "변수 이름은 대소문자를 구분한다.",
            "변수 이름에 한글을 사용할 수 있다."
        ]
    },
    {
        item_type: 'checkbox',
        question: '다음 중 Python 변수명이 될 수 있는 것을 모두 고르세요',
        options: [
            'my변수',
            '질문?',
            '가로 길이',
            '_v_',
            '3번째',
            'free4all'
        ],
        hint: '주피터 노트북에서 변수를 정의해보세요. 잘못된 이름이면 에러가 발생합니다.',
        solution: [
            'my변수',
            '_v_',
            'free4all'
        ],
    },
    {
        "item_type": "short",
        "question": "코드에 대한 설명이나 메모를 남기기 위해 사용하는 '주석'을 나타내는 기호는 무엇입니까?",
        "hint": "해시태그와 동일한 기호입니다.",
        "solution": "#"
    },
    {
        "item_type": "radio",
        "question": "다양한 함수가 모여있는 '모듈'을 코드에서 사용하기 위해 가장 먼저 실행해야 하는 명령어는 무엇입니까?",
        "options": [
            "include",
            "import",
            "export",
            "module"
        ],
        "hint": "'수입하다'라는 의미의 영어 단어입니다.",
        "solution": "import"
    },
    {
        "item_type": "radio",
        "question": "`math` 모듈을 `import`한 후, 4의 제곱근을 구하기 위한 올바른 코드는 무엇입니까?",
        "options": [
            "sqrt(4)",
            "math.sqrt(4)",
            "math(sqrt, 4)",
            "math->sqrt(4)"
        ],
        "hint": "모듈에 포함된 함수는 '모듈이름.함수이름' 형태로 사용합니다.",
        "solution": "math.sqrt(4)"
    },
    {
        "item_type": "radio",
        "question": "`math` 모듈에서 `sin` 함수만 가져와서 `math.` 접두사 없이 사용하고 싶을 때 올바른 문법은 무엇입니까?",
        "options": [
            "import sin from math",
            "from math use sin",
            "from math import sin",
            "import math.sin as sin"
        ],
        "hint": "'어떤 모듈로부터(from) 특정 함수를 가져온다(import)'는 순서로 작성합니다.",
        "solution": "from math import sin"
    }
]
} />


## Q&A
<iframe src="https://tally.so/embed/wbOOKg?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=0" loading="lazy" width="100%" height="274" frameborder="0" marginheight="0" marginwidth="0" title="Q&A"></iframe>