# Python 소개와 기초

## Python

  * 범용 프로그래밍 언어(General Purpose Programming Language)
  * 통계 이외에도 다양한 용도로 쓸 수 있음
  * 대표적인 용도가 웹개발: 유튜브, 인스타그램, 구글 등의 서비스가 모두 Python으로 개발
  * C나 Java등 다른 범용 언어에 비해 쉬움: 대학교 1학년 학생들에게 프로그래밍을 가르칠 때 사용
  * 전세계에서 가장 인기 있는 프로그래밍 언어

## Python과 데이터 분석

  * 데이터 과학 분야의 채용 공고에서 가장 많이 요구되는 프로그래밍 언어
  * 머신러닝 및 인공지능에서는 Python이 사실상의 표준(de facto standard)
  * ChatGPT는 자체적으로 Python 실행이 가능

## 주피터 노트북

  * 실행 방법: 시작 → Anaconda 3 → Jupyter Notebook
  * Python을 이용해 데이터 분석을 하기 위한 프로그램
  * 분석 서버에 웹으로 접속하여 구동하는 방식
  * PC에서 실행할 경우 PC 자체가 서버 역할
  * http://localhost:8888 에 접속 (localhost는 자기 자신)
  * 설치 없이 클라우드에서 사용할 수 있는 서비스들도 있음
      * 예: google colab

## 노트북 열기

  * 작업할 데이터 파일이 있는 폴더 선택
      * Desktop: 바탕화면
      * Documents: 내 문서
      * Downloads: 다운로드
  * 데이터 파일이 보이는지 확인
  * New → Notebook → Python 3

## 주피터 노트북 코드 실행

  * 단축키 SHIFT+ENTER
  * 코드 아래 실행 결과가 표시
      * 코드를 수정하고 다시 SHIFT+ENTER를 누르면 실행 결과를 덮어씀

## Python을 계산기처럼 사용하기

  * 덧셈 (`+`): `5+3`
  * 뺄셈 (`-`):
      * `10-4`
      * `4-6`
  * 곱셈 (`*`): `7*6`
  * 나눗셈 (`/`): `10/3`
  * 정수 나눗셈 (몫) (`//`): `10 // 3`
  * 나머지 (`%`): `10 % 3`
  * 거듭제곱 (`**`): `2 ** 4`
      * (2의 4제곱)

## 연산자 우선순위

  * 연산자 우선순위: 일반적인 수학의 연산 순서와 유사
      * 1.  `**` (거듭제곱)
      * 2.    `-` (음수 부호)
      * 3.  `*`, `/`, `//`, `%`
      * 4.  `+`, `-`
  * 예시: `2 + 3 * 4` → `2 + 12` → `14` (곱셈이 먼저)
  * 괄호 안의 표현식이 가장 먼저 계산
  * 예시 1: `(2 + 3) * 4` → `5 * 4` → `20` (괄호 안의 덧셈이 먼저)

## 변수

  * 변수: 데이터를 저장하는 공간
  * 데이터(값)에 이름을 붙이는 것
  * 변수는 값과 동일하게 사용할 수 있다

```python
x = 3
x + 1
```

  * 한 번 정의한 변수는 다시 정의할 수 있다

```python
x = 4
```

  * `=`의 오른쪽을 계산 후 왼쪽에 대입

```python
x = x + 1
```python
x = 4
```

  * `=`의 오른쪽을 계산 후 왼쪽에 대입

```python
x = x + 1
```

  * 변수 생성 규칙:
      * 한글, 영문자, 숫자, 밑줄(\_) 사용 가능
      * 숫자로 시작할 수 없음(x1은 가능, 1x는 불가능)
      * 대소문자 구분
      * 예약어 사용 불가 (e.g., if, for, while)

## 함수

  * 함수이름(인자) 형태로 사용.
  * 예시 (최대값): `max(1, 2, 3)`

## 주석

  * 한 줄의 코드에서 `#`를 표시 이후의 내용은 주석 처리
  * 코드에 설명이나 메모 등을 남기기 위한 방법

```python
1 + 1 # 더하기
```

## 퀴즈

import { QuizComponent } from "@/components/QuizComponent";

<QuizComponent quizId="intro" quizItems={
    [
  {
    "item_type": "checkbox",
    "question": "Python에 대한 설명으로 올바른 것을 모두 고르세요.",
    "options": [
      "범용 프로그래밍 언어이다",
      "통계 이외의 다른 용도로도 쓸 수 있다",
      "다른 언어에 비해 어렵다",
      "머신러닝과 인공지능에서는 사실상 표준 언어이다"
    ],
    "hint": "Python은 배우기 쉬우면서도 다양한 분야에서 활용되는 강력한 언어입니다.",
    "solution": [
      "범용 프로그래밍 언어이다",
      "통계 이외의 다른 용도로도 쓸 수 있다",
      "머신러닝과 인공지능에서는 사실상 표준 언어이다"
    ]
  },
  {
    "item_type": "radio",
    "question": "주피터 노트북에서 코드를 실행하는 단축키는?",
    "options": [
      "SHIFT + ENTER",
      "SHIFT + F10",
      "ALT + SPACE",
      "CTRL + INSERT"
    ],
    "hint": "현재 셀을 실행하고 다음 셀로 이동(또는 생성)하는 가장 일반적인 단축키입니다.",
    "solution": "SHIFT + ENTER"
  },
  {
    "item_type": "number",
    "question": "Python으로 `7 ** 3 + 5` 를 계산해보세요. 결과는 얼마입니까?",
    "options": [],
    "hint": "`**` 연산자는 거듭제곱을 의미합니다. (7의 3제곱)",
    "solution": 348
  },
  {
    "item_type": "radio",
    "question": "Python에서 주석을 표시하는 기호는?",
    "options": [
      "@",
      "#",
      "%",
      "&"
    ],
    "hint": "이 기호 뒤에 오는 텍스트는 해당 줄에서 실행되지 않습니다.",
    "solution": "#"
  },
  {
    "item_type": "radio",
    "question": "Python에서 `a = 5`의 의미는 무엇입니까?",
    "options": [
      "a와 5가 같다",
      "변수 a에 5를 대입하라"
    ],
    "hint": "`=` 기호는 할당 연산자입니다. '같다'는 비교 연산자는 `==` 입니다.",
    "solution": "변수 a에 5를 대입하라"
  },
  {
    "item_type": "checkbox",
    "question": "변수에 대한 설명으로 옳은 것을 모두 고르세요",
    "options": [
      "값에 이름을 붙인 것이다",
      "값과 동일하게 사용할 수 있다",
      "한 번 정의한 변수는 재정의할 수 없다"
    ],
    "hint": "변수는 데이터를 저장하는 공간의 이름표와 같습니다. 이 이름표는 다른 값에 다시 붙일 수 있습니다.",
    "solution": [
      "값에 이름을 붙인 것이다",
      "값과 동일하게 사용할 수 있다"
    ]
  },
  {
    "item_type": "checkbox",
    "question": "다음 중 Python 변수명이 될 수 있는 것을 모두 고르세요",
    "options": [
      "my변수",
      "질문?",
      "가로 길이",
      "_v_",
      "3번째",
      "free4all"
    ],
    "hint": "변수명은 영문자, 숫자, 밑줄(_)로 구성되며 숫자로 시작할 수 없습니다. 한글도 사용 가능하지만, 특수문자(?나 공백)는 사용할 수 없습니다.",
    "solution": [
      "my변수",
      "_v_",
      "free4all"
    ]
  },
  {
    "item_type": "number",
    "question": "`max(1, 2, 3)`의 결괏값은 얼마입니까?",
    "options": [],
    "hint": "`max()` 함수는 주어진 값들 중 가장 큰 값을 반환합니다.",
    "solution": 3
  }
]
} />


## Q&A
<iframe src="https://tally.so/embed/wbOOKg?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=0" loading="lazy" width="100%" height="274" frameborder="0" marginheight="0" marginwidth="0" title="Q&A"></iframe>