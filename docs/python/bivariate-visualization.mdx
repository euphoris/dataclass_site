# 두 변수의 관계 시각화

## 산점도
* 각각의 사례를 점으로 표시
```python
sns.scatterplot(x='mileage', y='price', data=df)
```

## 연식을 점의 크기로
```python
sns.scatterplot(x='mileage', y='price', size='year', data=df)
```

## 추세선 추가
```python
sns.lmplot(x='mileage', y='price', data=df)
```

## 등고선 추가
* 두 줄을 한 셀에
```python
sns.scatterplot(x='mileage', y='price', data=df)
sns.kdeplot(x='mileage', y='price', data=df, color='lightgray', alpha=0.5)
```

## 선 그래프
* `sns.lineplot(x='x축_열이름', y='y축_열이름', data=데이터프레임)`
* 용도: 시간의 흐름에 따른 데이터 변화, 연속적인 값들의 추세 시각화
* 주요 인자:
    * `x`: x축에 해당하는 데이터프레임의 열 이름
    * `y`: y축에 해당하는 데이터프레임의 열 이름
    * `data`: 사용할 데이터프레임 객체
    * `hue`: 특정 열의 값에 따라 선 색상 구분
    * `estimator`: y값의 집계 방법 (기본값은 평균)
```python
sns.lineplot(data=df, x='year', y='price') # 연도별 가격
sns.lineplot(data=df, x='year', y='price', hue='model') # 연도별 모델별 가격
sns.lineplot(data=df, x='year', y='price', estimator='std') # 연도별 표준편차
```

## 막대 그래프
* `sns.barplot(x='x축_범주형열', y='y축_수치형열', data=데이터프레임)`:
    * 범주형 데이터에 따른 수치형 데이터의 크기(예: 평균, 합계 등) 비교
```python
sns.barplot(data=df, x='model', y='price') # 모델별 가격 평균
```
* `sns.countplot(x='x축_범주형열', data=데이터프레임)`:
    * 범주형 데이터의 각 항목별 빈도수(개수) 시각화 (y축 별도 지정 불필요)
```python
sns.countplot(data=df, x='model') # 모델별 개수
```

## 퀴즈

import { QuizComponent } from "@/components/QuizComponent";

<QuizComponent quizId="bivariate-visualization" quizItems={
[
    {
        "item_type": "radio",
        "question": "두 개의 연속적인 숫자 변수(예: 주행거리 'mileage'와 가격 'price') 사이의 관계를 각 데이터를 점으로 표시하여 시각화하는 데 가장 적합한 그래프는 무엇입니까?",
        "options": [
            "산점도 (Scatter Plot)",
            "막대 그래프 (Bar Plot)",
            "선 그래프 (Line Plot)",
            "히스토그램 (Histogram)"
        ],
        "hint": "seaborn에서는 `scatterplot()` 함수를 사용하여 그릴 수 있습니다.",
        "solution": "산점도 (Scatter Plot)"
    },
    {
        "item_type": "radio",
        "question": "`sns.scatterplot()`에서 '연식(year)'과 같은 세 번째 변수를 점의 '크기'로 표현하고 싶을 때 사용하는 인자는 무엇입니까?",
        "options": [
            "hue='year'",
            "style='year'",
            "size='year'",
            "width='year'"
        ],
        "hint": "'크기'를 의미하는 영어 단어를 인자 이름으로 사용합니다.",
        "solution": "size='year'"
    },
    {
        "item_type": "short",
        "question": "산점도와 함께 데이터의 추세를 나타내는 회귀선을 함께 그려주는 seaborn 함수는 무엇입니까?",
        "hint": "'linear model'과 'plot'의 약어를 합친 이름입니다.",
        "solution": "lmplot"
    },
    {
        "item_type": "radio",
        "question": "시간의 흐름(예: 'year')에 따른 특정 값(예: 'price')의 변화나 추세를 시각화하는 데 가장 적합한 그래프는 무엇입니까?",
        "options": [
            "선 그래프 (Line Plot)",
            "산점도 (Scatter Plot)",
            "상자 그림 (Box Plot)",
            "파이 차트 (Pie Chart)"
        ],
        "hint": "seaborn에서는 `lineplot()` 함수를 사용하며, 기본적으로 x축 값에 따른 y축 값의 평균을 선으로 연결합니다.",
        "solution": "선 그래프 (Line Plot)"
    },
    {
        "item_type": "radio",
        "question": "`sns.lineplot`은 기본적으로 y값의 평균을 계산하여 보여줍니다. 만약 평균 대신 표준편차('std')를 계산하여 보여주고 싶다면 어떤 인자를 사용해야 합니까?",
        "options": [
            "agg='std'",
            "func='std'",
            "stat='std'",
            "estimator='std'"
        ],
        "hint": "'추정량'을 의미하는 영어 단어를 인자 이름으로 사용합니다.",
        "solution": "estimator='std'"
    },
    {
        "item_type": "radio",
        "question": "범주형 데이터('model')에 따른 수치형 데이터('price')의 평균값을 막대로 비교하고 싶을 때 사용하는 함수는 무엇입니까?",
        "options": [
            "sns.histplot()",
            "sns.countplot()",
            "sns.barplot()",
            "sns.boxplot()"
        ],
        "hint": "이 함수는 x축과 y축을 모두 지정해야 합니다.",
        "solution": "sns.barplot()"
    },
    {
        "item_type": "radio",
        "question": "범주형 데이터('model')의 각 항목이 몇 개씩 있는지 그 빈도(개수)를 막대그래프로 시각화하고 싶을 때 사용하는 함수는 무엇입니까?",
        "options": [
            "sns.histplot()",
            "sns.countplot()",
            "sns.barplot()",
            "sns.lineplot()"
        ],
        "hint": "이 함수는 y축을 별도로 지정할 필요 없이 x축의 데이터 개수를 자동으로 세어줍니다.",
        "solution": "sns.countplot()"
    }
]
} />


## Q&A
<iframe src="https://tally.so/embed/wbOOKg?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=0" loading="lazy" width="100%" height="274" frameborder="0" marginheight="0" marginwidth="0" title="Q&A"></iframe>